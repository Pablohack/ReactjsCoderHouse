import { useState } from "react"

const input =({})=>{

    return(
        <div>
            <h4>agregar item</h4>
            <input type="text"/>
            <button>Agregar cantidad</button>
        </div>
    )
}
export default input